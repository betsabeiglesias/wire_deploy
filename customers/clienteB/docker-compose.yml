services:
  gateway:
    build: ../../gateway
    container_name: gateway_customerB
    env_file:
      - ./customerB.env
    environment:
      - TZ=Europe/Madrid
      - GATEWAY_CONFIG=/opt/suite/config/gateway.yaml
    volumes:
      - ./opt/suite/config:/opt/suite/config:ro
    restart: unless-stopped
    networks:
      - suite-central-net

  telegraf:
    image: telegraf:1.30
    container_name: telegraf_customerB
    restart: unless-stopped
    # depends_on:
      # - mosquitto
      # - influxdb
    env_file:
      - ./customerB.env
    environment:
      HOSTNAME: telegraf_customerB
    volumes:
      - ./telegraf/telegraf_customerB.conf:/etc/telegraf/telegraf.conf:ro
    networks:
      - suite-central-net


networks:
  suite-central-net:
    external: true
    name: suite-central-net
